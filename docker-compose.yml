services:
  mylists:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - mylists-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/mylists.db
      - TMDB_API_KEY=${TMDB_API_KEY}

volumes:
  mylists-data:
